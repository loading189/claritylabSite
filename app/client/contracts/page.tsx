import { Card } from '@/components/Card';

export default function ClientContractsPage() {
  return (
    <Card title="Contracts">
      <p>Contract storage is enabled for v1 placeholders. You can also use this area for engagement documents and SOW addenda.</p>
    </Card>
  );
}
